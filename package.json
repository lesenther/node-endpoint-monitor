{
  "name": "endpoint-monitor",
  "version": "1.0.0",
  "description": "Monitors an endpoint and does something if it changes",
  "author": "Logan Esenther (logan@cal.berkeley.edu)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1"
  }
}
